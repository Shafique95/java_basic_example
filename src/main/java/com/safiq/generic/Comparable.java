package main.java.com.safiq.generic;

import java.util.ArrayList;
import java.util.TreeSet;

public interface  Comparable<T> {
    public int compareTo(T o);
    ArrayList arrayList =new ArrayList<>();
    TreeSet tr=new TreeSet(null);
    }
